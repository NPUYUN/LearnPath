# 修改点
本文档用来记录修改的地方，便于后续的开发和测试

## v1.0.1
- 删去了界面的冗余成分
- 新增了说明文档，供用户阅读
- 优化了资源读取，生成的界面和逻辑
- 优化了学习路径生成的界面和逻辑

## v1.0.2
- 重构 AI 课堂流程推进逻辑，形成“导入—讲解—例子—小测—反馈—总结”逻辑
- 优化课堂互动按钮逻辑，使五个互动按钮均能触发明确状态变化
- 新增课堂阶段进度条与课堂运行状态记录
- 增强课堂总结，展示掌握点、卡点、互动次数与下一步建议
- 完善课中小测验作答、判题、解析与状态回流逻辑
- 优化 AI 课堂题目生成逻辑

## v1.0.3
- 优化学习资源生成质量，完善讲义、题集、代码案例等资源标准
- 增强资源质量审核，过滤低质内容、错误公式、答案冲突和不可运行代码
- 修复数学公式、旧题集和资源详情显示问题
- 优化资料库文件预览、资源归属和资源清单同步逻辑
- 重构资源生成弹窗，并支持后台任务进度和结果摘要
- 支持生成资源自动加入学习路径和 AI 课堂复用
- 优化 AI 课堂讲解、互动、快捷提问和课堂状态提示
- 增加掌握确认、错因记录、薄弱页回看和课后复习卡
- 完善课堂总结、PPT 导出和旧数据兼容
- 补充资源审核、资料库同步和课堂问答自动化测试

## v1.0.4（P0 体验闭环 · 2026-06）

### P0-1 掌握度反馈
- **后端**：新增 `mastery_service.py`、`/api/mastery/feedback`、`/api/mastery/{user_id}/records`；掌握度三档（一般 / 较好 / 很好）写入用户偏好并计算下次复习间隔
- **前端**：新增 `MasteryFeedbackBar.tsx`、`masteryStorage.ts`；**仅在学习路径页**展示三档反馈，预览/详情/课堂页已移除
- **路径步骤**：恢复「标记完成」按钮，调用 `updatePathStep(..., "done")`；掌握度反馈**不再自动**将路径步骤标为完成

### P0-2 专属复习卡
- **后端**：新增 `review_card_service.py`、独立路由 `review_cards.py`（`GET/POST /api/review-cards`）；按大主题 LLM 生成专属复习卡并持久化到资源库（`type=review_card`）；修复生成 500（`get_user_resources` 返回 Pydantic 对象不可 `.get()`）
- **前端**：新增 `ReviewCardsPanel.tsx`、`ReviewCardGenerateModal.tsx`、`reviewCardTopics.ts`；资源库新增「复习卡」Tab（无数量后缀）；按大主题文件夹分组、**默认收起**；联想仅显示大主题
- **存储策略**：复习卡**必须存后端**，已删除本地 `reviewCardStorage` / `reviewCardContent` 兜底；生成失败直接报错提示重启后端
- **资源类型**：`resourceConfig` 增加 `review_card`（专属复习卡）

### P0-3 侧栏打卡与今日最小任务
- **后端**：`eval_stats_service.py` 抽取评估统计逻辑；`EvalStats` 新增 `study_streak`（连续学习天数）、`studied_today`（今日是否已学）
- **侧栏**：新增 `SidebarStudyStats.tsx`（连续天数、累计天数、今日计划进度条）；`AppSidebar` 接入并与 `SidebarDailyPlan` 同步进度
- **路径页**：新增 `PathDailyMinimumCard.tsx`、`dailyMinimumTasks.ts`；展示三项今日最小任务（路径步骤 / 复习卡 / 小测）；复习资源点击跳转对应复习卡全屏页；小测验暂提示「即将上线」

### 全屏资源与初始化
- **跳转缓存**：新增 `resourceViewCache.ts`（sessionStorage 预热 + store 回退）；跳转全屏前写入缓存，避免首次加载「资源不存在」
- **AppShell**：进入 `/resources/view/...` 时跳过 init 遮罩、避免预热被 router 打断导致卡死
- **ResourceDetailPage**：优先使用本地缓存展示，仍请求后端刷新

### 运维脚本（Windows）
- **`scripts/lib/common.ps1`**：停止时清理 `spawn_main` / `multiprocessing-fork` 等 uvicorn 孤儿 worker；增强 8000/3000 端口释放
- **`start.bat` / `stop.bat` / `scripts/stop.ps1`**：与 common.ps1 对齐，彻底清理 8000 端口残留进程

### Bug 修复
- **侧栏无限循环**：`Maximum update depth exceeded`（Ant Design Typography Ellipsis 连锁触发）；`AppSidebar` 用 `useCallback` 稳定 `onStatsChange`；`SidebarDailyPlan` 用 ref 存回调避免 effect 依赖变化；按钮内 `Typography.Text` 改为原生 `span` 避免 ellipsis 布局循环
- **今日最小任务跳转**：复习资源原用 `clientNavigate` 无法打开 `/resources/view/{id}`，改为 `useRouter().push` 与资源库一致

### 涉及主要文件

| 类别 | 路径 |
|------|------|
| 掌握度 API | `backend/app/api/routes/mastery.py`、`backend/app/services/mastery_service.py` |
| 复习卡 API | `backend/app/api/routes/review_cards.py`、`backend/app/services/review_card_service.py` |
| 评估统计 | `backend/app/services/eval_stats_service.py`、`backend/app/api/routes/eval.py` |
| 侧栏打卡 | `frontend/src/components/SidebarStudyStats.tsx`、`AppSidebar.tsx` |
| 今日计划 | `frontend/src/components/SidebarDailyPlan.tsx`、`PathDailyMinimumCard.tsx` |
| 复习卡 UI | `frontend/src/components/ReviewCardsPanel.tsx`、`pages/ResourcesContent.tsx` |
| 全屏缓存 | `frontend/src/lib/resourceViewCache.ts`、`ResourceDetailPage.tsx`、`AppShell.tsx` |
| 运维 | `scripts/lib/common.ps1`、`start.bat`、`stop.bat` |

### 验证建议
1. `stop.bat` → `start.bat`，确认 `/api/health` 含 `review_cards: true`
2. 资源库「复习卡」Tab：生成、文件夹默认收起、全屏阅读
3. 学习路径：三档掌握度 + 标记完成；今日最小任务点击复习卡可跳转
4. 侧栏：连续天数与今日计划进度正常，无 React 无限更新报错

---
title: 数据结构
chapter: 2
---

# 数据结构

## 列表 list

有序、可变。常用操作：`append`、`extend`、`pop`、切片 `lst[a:b]`。

## 字典 dict

键值映射，键须可哈希。遍历：`for k, v in d.items()`。

## 集合 set

无序、不重复元素，适合去重与集合运算。

## 推导式

列表推导式 `[f(x) for x in xs if cond]` 与字典推导式可简洁构造数据结构。

## 与算法学习的关系

掌握基础数据结构后，可进一步学习排序、查找与机器学习中的向量表示。
